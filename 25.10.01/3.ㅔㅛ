def print_n_times(n, *values):
    # n번 반복합니다.
    for i in range(n):
        for value in values:
            print(value)
            # 단순한 줄바꿈
        print()

# 함수를 호출합니다.
print_n_times(5, '안녕하세요', '반갑습니다', '행복하세요')
